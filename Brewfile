# =============================================================
# AIDENY — Brewfile
# Homebrew dependencies for the denial engine
# =============================================================
# Install everything: brew bundle
# This installs one thing. The Brewfile is 20 lines.
# The ratio is on-brand.
# =============================================================

# The only actual dependency
brew "gnucobol"

# Optional: tools for the infrastructure that nobody asked for
brew "docker"
brew "kubectl"
brew "helm"
brew "terraform"
brew "hadolint"
brew "shellcheck"
brew "pre-commit"
